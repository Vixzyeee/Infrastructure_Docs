# curl and wget

## Purpose
Interact with network services from the command line.

## Core Concept
`curl` and `wget` fetch data over network protocols
such as HTTP and HTTPS.

`curl` is more flexible and script-friendly.
`wget` is simple and good for downloads.

## Engineer Notes
Curl is indispensable for API testing
and service debugging.

If you work in infrastructure and don’t know curl,
you are handicapped.

## Summary
- Fetch data from network services
- curl is powerful and flexible
- wget is simple and reliable
