# rm

## Purpose
Remove files and directories.

## Core Concept
The `rm` command deletes files permanently.
The `-r` option removes directories recursively.

There is no undo.

## Engineer Notes
Double-check paths before using `rm -r`.
Most irreversible mistakes start here.

## Summary
- Deletes files and directories
- Actions are permanent
