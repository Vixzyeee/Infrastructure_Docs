# Python Version

This roadmap uses Python 3.

Reasons:
- Python 2 is end-of-life
- Python 3 is the current industry standard
- Supported by modern libraries and tooling

Check installed version:

```bash
python3 --version
```

Example output:

```text
Python 3.x.x
```
